// Crie uma função que recebe uma palavra (String) e uma letra (String)
// A função deve retornar a quantidade de vezes que a letra aparece na palavra (Number)

const contaOcorrencias = (palavra, letra) => {
  return 
}

export default [
  'Exercício 1: Conta Ocorrências',
  contaOcorrencias('Maria', 'a') === 2,
  contaOcorrencias('José', 'a') === 0,
  contaOcorrencias('Roberto', 'be') === 0,
]